#!/bin/sh

set -e

cd $XDG_CONFIG_HOME/emacs/straight/build/vterm
rm -rf build
mkdir build
cd build
cmake ..
make 2>/dev/null

nvim libvterm-prefix/src/libvterm/Makefile
# Remove conditional at top of file so `LIBTOOL ?= libtool
# make
